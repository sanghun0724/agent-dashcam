"""Agent Dashcam adapters — vendor-specific session log parsers.

Each submodule (claude, codex, gemini) normalizes one CLI's native session log
into the shape Agent Dashcam's scorer expects. See docs/ARCHITECTURE.md.
"""
